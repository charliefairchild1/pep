"""pep.strata — markets-primitive engines.

All verticals share the same core: assets as nodes, correlation as edges,
momentum as spreading activation, surprise as residual scoring, regime as
state modulation. Each vertical supplies its own asset universe and
archetype definitions.
"""
